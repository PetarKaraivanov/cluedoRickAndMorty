# Copy this file to .env.local and fill in your values.

# The single shared password every friend needs to enter on /login.
APP_PASSWORD=changeme

# HMAC secret used to sign the auth cookie. Use a long random string.
AUTH_SECRET=please-change-this-to-a-long-random-string

# Optional: override the port the custom server listens on.
PORT=3000
